# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Database
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13

# Validation / config
pydantic>=2.7
pydantic-settings>=2.3

# Cache / queue
redis>=5.0
arq>=0.26

# HTTP client
httpx>=0.27

# Auth / crypto
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
cryptography>=42

# Payments
stripe>=10

# Form data / file uploads
python-multipart>=0.0.9

# GPX parsing (for activity import)
gpxpy>=1.6

# Object storage (S3-compatible: AWS, Hetzner, MinIO, R2)
# Optional: only needed when STORAGE_BACKEND != "db"
aiobotocore>=2.13

# API documentation
scalar-fastapi>=1.8

# Testing
respx>=0.21
pytest>=8
pytest-asyncio>=0.23
aiosqlite>=0.20
